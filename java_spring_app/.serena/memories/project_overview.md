# Project Overview

ITS (Issue Tracking System) is a Spring Boot 3.2.1 web application built with Java 21 for managing project issues and tasks. The system provides user authentication with Spring Security 6 and uses H2 in-memory database for development.
